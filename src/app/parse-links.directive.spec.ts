import { ParseLinksDirective } from './parse-links.directive';

describe('ParseLinksDirective', () => {
  it('should create an instance', () => {
    const directive = new ParseLinksDirective();
    expect(directive).toBeTruthy();
  });
});
